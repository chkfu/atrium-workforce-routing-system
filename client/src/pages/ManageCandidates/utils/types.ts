export enum enum_gender {
  male = 'male',
  female = 'female',
  other = 'other',
}

export enum enum_prob_status {
  selecting = 'selecting',
  training = 'training',
  completed = 'completed',
  postponed = 'postponed',
  withdrawn = 'withdrawn',
  failed = 'failed',
}
